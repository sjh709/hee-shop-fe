export type Register = {
  email: string;
  name: string;
  password: string;
  confirmPassword: string;
  policy: boolean;
};

export type User = {
  email: string;
  name: string;
  password: string;
};
